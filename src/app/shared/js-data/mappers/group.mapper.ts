import { groupSchema } from '../schemas'
import { groupRelation} from "../relations";

export const groupMapper = {
  schema: groupSchema,
  relations: groupRelation,
  // Function can also be defined here
};
